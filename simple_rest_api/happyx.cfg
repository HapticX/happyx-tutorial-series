# HappyX project configuration.

[Main]
projectName = simple_rest_api
projectType = SSG
mainFile = main  # main script filename (without extension) that should be launched with hpx dev command
srcDir = src  # source directory
